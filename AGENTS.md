# 项目维护规则

## “新内容”维护

- 每次正式发布新版本时，同步更新 `newtab/newtab.html` 中的“新内容”页面。
- 更新内容一律使用中文，按版本号从新到旧排列，并写明准确的版本号和发布日期。
- 只记录已经完成验收、确认达到预期的版本；调试版、中间尝试和未达预期版本不得写入。
- 不得恢复上游 `v2.2.6 - 2026-01-14` 的旧更新内容。
- `manifest.json` 与 `package.json` 的版本号发生变化时，必须检查“新内容”是否需要同步更新。
